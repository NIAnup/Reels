import 'package:flutter/material.dart';

class Messaage extends StatelessWidget {
  const Messaage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text("Message"),
    ));
  }
}
